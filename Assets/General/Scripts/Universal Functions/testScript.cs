using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class testScript : UniversalHealthSystem
{
    //The health assets and variables that will be used derive from here
    /*private Health health;
    // Start is called before the first frame update
    void Start()
    {
        GameObject mc = GameObject.Find("Universal Health Tester");
        UniversalHealth mark = mc.GetComponent<UniversalHealth>();
        mark.GetHealthDecrease().AddListener(DamageScaler);
    }

    void DamageScaler(float damage)
    {
        damage = 1f;
    }*/
}
